<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Encabezado -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Traductor de voz"
            android:textColor="?attr/colorOnSurface"
            android:textSize="26sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:alpha="0.7"
            android:text="Traducción instantánea · sin internet"
            android:textColor="?attr/colorOnSurface"
            android:textSize="14sp" />

        <!-- Tarjeta de resultado -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            app:cardBackgroundColor="?attr/colorPrimaryContainer"
            app:cardCornerRadius="24dp"
            app:cardElevation="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView
                    android:id="@+id/label_original"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:alpha="0.7"
                    android:letterSpacing="0.06"
                    android:text="ORIGINAL"
                    android:textAllCaps="true"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:textSize="12sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/text_original"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="—"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:textSize="18sp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/button_leer_original"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:layout_marginTop="2dp"
                    android:minWidth="0dp"
                    android:text="Leer"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    app:icon="@drawable/ic_volume"
                    app:iconGravity="textStart"
                    app:iconTint="?attr/colorOnPrimaryContainer" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:layout_marginVertical="16dp"
                    android:alpha="0.25"
                    android:background="?attr/colorOnPrimaryContainer" />

                <TextView
                    android:id="@+id/label_traduccion"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:alpha="0.7"
                    android:letterSpacing="0.06"
                    android:text="TRADUCCIÓN"
                    android:textAllCaps="true"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:textSize="12sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/text_traduccion"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:lineSpacingMultiplier="1.1"
                    android:text="—"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:textSize="26sp"
                    android:textStyle="bold" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/button_leer_traduccion"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:layout_marginTop="2dp"
                    android:minWidth="0dp"
                    android:text="Leer"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    app:icon="@drawable/ic_volume"
                    app:iconGravity="textStart"
                    app:iconTint="?attr/colorOnPrimaryContainer" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Estado -->
        <TextView
            android:id="@+id/text_estado"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="Preparando el traductor…"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.75"
            android:textSize="14sp" />

        <!-- Modo conversación: un solo toque, alterna español/inglés automáticamente -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_conversacion"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/btn_ocean"
            android:text="Modo conversación (automático)"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@null"
            app:icon="@drawable/ic_mic"
            app:iconGravity="textStart"
            app:iconTint="@color/white" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:alpha="0.55"
            android:gravity="center"
            android:text="— o traduce una sola frase —"
            android:textColor="?attr/colorOnSurface"
            android:textSize="12sp" />

        <!-- Botón 1: Español -> Inglés -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_es_en"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/btn_ocean"
            android:text="Yo hablo   ·   Español → Inglés"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@null"
            app:icon="@drawable/ic_mic"
            app:iconGravity="textStart"
            app:iconTint="@color/white" />

        <!-- Botón 2: Inglés -> Español -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_en_es"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/btn_teal"
            android:text="Ellos hablan   ·   Inglés → Español"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@null"
            app:icon="@drawable/ic_mic"
            app:iconGravity="textStart"
            app:iconTint="@color/white" />

        <!-- Separador -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="20dp"
            android:alpha="0.15"
            android:background="?attr/colorOnSurface" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Documentos"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Botón: escanear documento con la cámara -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_scan"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginTop="10dp"
            android:text="Escanear documento y traducir"
            android:textSize="16sp"
            app:cornerRadius="14dp"
            app:icon="@drawable/ic_camera"
            app:iconGravity="textStart" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:alpha="0.55"
            android:gravity="center"
            android:text="Toca y habla, o escanea un documento. Todo se lee en voz alta y funciona sin internet."
            android:textColor="?attr/colorOnSurface"
            android:textSize="13sp" />
    </LinearLayout>
</androidx.core.widget.NestedScrollView>
